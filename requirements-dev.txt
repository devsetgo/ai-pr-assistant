-r requirements.txt
pytest>=9.1.1,<10
pytest-cov>=7.1.0,<8
# Keep in sync with `rev` in .pre-commit-config.yaml (both must run the same ruff).
ruff==0.16.8
pre-commit>=4.6.2,<5
genbadge[all]>=1.1.3,<2
# `make test` / `make typecheck` run mypy; pinned since a new release can add errors.
mypy==2.3.1
zensical==0.0.63
bumpcalver==2026.9.19.2
